# Dotfiles

Just my dotfiles for myself.

1. clone the repo
2. run install.sh

### Steps

```
git clone https://github.com/hanhwanglim/dotfiles.git ~/dotfiles
cd ~/dotfiles
chmod +x install.sh
./install.sh
```
